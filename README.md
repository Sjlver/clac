A small web frontend to label CVE entries

Installation
------------

        virtualenv -p python3 venv
        source venv/bin/activate

        pip3 install -r requirements.txt

Run
---

        cd clac
        python manage.py runserver
